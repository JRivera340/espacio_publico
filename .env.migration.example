# Credenciales de SOLO LECTURA de la base del hub. Las usa
# `npm run migrate:legacy` para copiar las actividades historicas de Espacio
# Publico. Este script nunca escribe en el hub.
OLD_DB_HOST=
OLD_DB_PORT=5432
OLD_DB_USERNAME=
OLD_DB_PASSWORD=
OLD_DB_DATABASE=
OLD_DB_SSL=true
